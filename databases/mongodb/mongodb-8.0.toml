name = "mongodb"
version = "8.0.15"
type = "database"

homepage = "https://www.mongodb.com"
license = "SSPL-1.0"

# Server launched by Cove via absolute path; mongod creates its dbpath on first run.
bin = [
  "bin/mongod.exe",
]

[service]
name = "cove-mongodb"
port = 27017

[architecture.x64]
url = "https://fastdl.mongodb.org/windows/mongodb-windows-x86_64-8.0.15.zip"
hash = ""
